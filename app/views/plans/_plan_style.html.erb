<div class="topbox4">
    <div>
      <span>並び替え:</span>
      <span><%= sortable "title", "タイトル順" %>-</span>
      <span><%= sortable "startday", "予定開始日時順" %>-</span>
      <span><%= sortable "endday", "予定終了日時順" %></span>
    </div>
      
    <% @plans.each do |plan| %>
      <%= link_to plan_path(plan) do %>
        <p>
          <span><%= plan.title %></span><br>
          <span><%= plan.startday.strftime("%Y年%m月%d日 %H時%M分") %>　～</span>
          <span><%= plan.endday.strftime("%Y年%m月%d日 %H時%M分") %></span><br>
          <span>投稿者:<%= plan.user.name %></span>
        </p>
      <% end %>
    <% end %>
  </div>
</div>